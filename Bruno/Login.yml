info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5000/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "abc@example.com",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

runtime:
  scripts:
    - type: after-response
      code: |-
        if (res.body && res.body.token) {
          bru.setEnvVar("token", res.body.token);
          console.log("Token saved to environment:", res.body.token);
        }

examples:
  - name: login
    request:
      url: http://localhost:5000/api/auth/login
      method: POST
      body:
        type: json
        data: |-
          {
            "email": "abc@example.com",
            "password": "password123"
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: success
