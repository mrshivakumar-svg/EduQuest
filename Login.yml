info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5000/api/auth/logins
  body:
    type: json
    data: |-
      {
        "email": "abc@example.com",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
